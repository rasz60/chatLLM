# 2026-04-09 작업 내역

## 커밋 이력

### `a85e388` — 챗봇 소스 수정
**시간:** 2026-04-09 17:58

#### 작업 내용
프로젝트 전체 구조 리팩토링 — 단일 `main.go`에서 핸들러/서비스/설정 분리, 프론트엔드 App 레이아웃 신규 구성.

#### 변경된 파일 (17개, +1443 / -478)
| 파일 | 내용 |
|------|------|
| `.gitignore` | 백엔드 빌드 아티팩트 및 환경파일 제외 추가 |
| `README.md` | 프로젝트 설명 전면 재작성 |
| `backend/.env.example` | 환경 변수 항목 정비 |
| `backend/config/config.go` | **신규** — 환경변수 로딩 설정 구조체 (`Config`) |
| `backend/db/db.go` | **신규** — DB 연결 및 테이블 마이그레이션 (`Migrate()`) |
| `backend/handlers/chat.go` | **신규** — 채팅 HTTP 핸들러 분리 (`ChatHandler`) |
| `backend/main.go` | 핸들러/서비스 초기화 라우팅만 남기고 대폭 축소 (-278줄) |
| `backend/middleware/cors.go` | **신규** — CORS 미들웨어 |
| `backend/models/types.go` | **신규** — 공유 타입 정의 (`Message`, `ChatRequest` 등) |
| `backend/services/llm.go` | **신규** — LLM API 호출 서비스 레이어 (`LLMService`) |
| `backend/services/search.go` | **신규** — 문서 검색 서비스 (`SearchService`) |
| `backend/upload_documents.py` | **신규** — 문서 임베딩 업로드 스크립트 (Python) |
| `frontend/src/App.css` | **신규** — 앱 전체 레이아웃 스타일 (헤더, 메뉴, 플로팅 버튼) |
| `frontend/src/App.tsx` | **신규** — 루트 App 컴포넌트 (헤더, 드롭다운 메뉴, 스크롤 감지) |
| `frontend/src/ChatWidget.css` | 채팅 위젯 스타일 대폭 확장 (+271줄) |
| `frontend/src/ChatWidget.tsx` | 채팅 위젯 기능 개선 (마크다운 렌더링, UX 개선) |
| `frontend/src/main.tsx` | 진입점 정리 |

#### 주요 아키텍처 변경
- **Backend:** `main.go` 모놀리식 → `config/`, `db/`, `handlers/`, `middleware/`, `models/`, `services/` 패키지 분리
- **Frontend:** `ChatWidget`만 있던 구조 → `App.tsx`가 루트 레이아웃 담당, ChatWidget은 임베딩 위젯으로 역할 분리
- **LLM 서비스:** LLM API 호출 로직을 `services/llm.go`로 분리
- **문서 검색:** 벡터 검색 기능 `services/search.go`로 분리

---

### `8438862` — ✔ 작업 내용 README 정리
**시간:** 2026-04-09 18:00

#### 작업 내용
README 문서 내용 보완 및 정리.

#### 변경된 파일
- `README.md` — 내용 추가/수정 (+32줄 / -15줄)

---

### `558d34a` — 한국어 모델 변경
**시간:** 2026-04-09 18:29

#### 작업 내용
LLM 모델을 한국어 특화 모델로 교체, 관련 설정 및 코드 정비.

#### 변경된 파일 (6개)
| 파일 | 내용 |
|------|------|
| `README.md` | 모델 변경 반영 문서 업데이트 |
| `backend/.env.example` | `LLM_MODEL` 기본값 변경 |
| `backend/config/config.go` | 기본 모델명 상수 변경 |
| `backend/services/llm.go` | 한국어 모델 호출 로직 조정 (+29줄 / -22줄) |
| `frontend/src/App.css` | 스타일 미세 조정 |
| `frontend/src/App.tsx` | 모델 관련 UI 레이블 수정 |
