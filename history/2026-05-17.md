# 2026-05-17 작업 내역

## 개요
업무일지(WorkReport)와 급여명세서(SalaryReport) 메뉴 2개를 추가.  
`E:\jwkim` (Python Flask + MongoDB + 순수 HTML) 소스를 chatLLM 프로젝트 (Go + PostgreSQL + React/TS) 에 맞게 전면 리팩토링하여 통합.

---

## 신규 생성 파일

### Backend
| 파일 | 내용 |
|------|------|
| `backend/handlers/utils.go` | `jsonResponse()` 공통 헬퍼 함수 |
| `backend/handlers/work.go` | 업무일지 CRUD 핸들러 (`WorkHandler`) |
| `backend/handlers/salary.go` | 급여명세서 핸들러 — chromedp 헤드리스 Chrome 자동화 (`SalaryHandler`) |

### Frontend
| 파일 | 내용 |
|------|------|
| `frontend/src/pages/WorkReport.tsx` | 업무일지 React 컴포넌트 (필터, 테이블, 모달, 마크다운 에디터, 페이지네이션) |
| `frontend/src/pages/WorkReport.css` | 업무일지 스타일 (`.wr-` 접두사 스코프) |
| `frontend/src/pages/SalaryReport.tsx` | 급여명세서 React 컴포넌트 (연/월 선택, 비밀번호 입력, 간략/전체 조회, 파일 업로드) |
| `frontend/src/pages/SalaryReport.css` | 급여명세서 스타일 (`.sr-` 접두사 스코프) |
| `frontend/src/vite-env.d.ts` | Vite CSS 사이드이펙트 import 타입 선언 (누락 파일 복구) |

### 기타
| 파일 | 내용 |
|------|------|
| `history/2026-04-08.md` | 프로젝트 초기화 커밋 이력 정리 |
| `history/2026-04-09.md` | 챗봇 소스 리팩토링 커밋 이력 정리 |
| `history/2026-04-10.md` | 채팅 모델/UI 변경 커밋 이력 정리 |

---

## 수정된 파일

### Backend
| 파일 | 내용 |
|------|------|
| `backend/models/types.go` | `Work`, `WorkCreateRequest`, `WorkReadRequest`, `WorkUpdateRequest`, `WorkListResponse` 타입 추가 |
| `backend/db/db.go` | `works` 테이블 마이그레이션 추가 |
| `backend/config/config.go` | `SalaryDir string` 필드 추가 (환경변수 `SALARY_DIR`) |
| `backend/main.go` | `workHandler`, `salaryHandler` 초기화 및 8개 라우트 등록 |
| `backend/go.mod` | `github.com/chromedp/chromedp v0.15.1` 추가 |
| `backend/go.sum` | chromedp 의존성 해시 추가 |
| `backend/.env` | `SALARY_DIR=E:\jwkim\backend\_data\salary` 추가 |

### Frontend
| 파일 | 내용 |
|------|------|
| `frontend/src/App.tsx` | `currentPage` 상태 기반 라우팅, 업무일지/급여명세서 메뉴 항목 추가 |
| `frontend/package.json` | `"marked": "^12.0.0"` 의존성 추가 |
| `frontend/package-lock.json` | 의존성 잠금 파일 갱신 |

---

## 주요 기술 결정

### Python → Go 포팅
| 항목 | E:\jwkim (기존) | chatLLM (포팅) |
|------|----------------|----------------|
| 헤드리스 브라우저 | Python Playwright | Go `chromedp` |
| DB | MongoDB | PostgreSQL (기존 DB 재사용) |
| 라우팅 | Flask Blueprint | `net/http` 핸들러 |
| 프론트엔드 | 순수 HTML/JS | React 18 + TypeScript |

### API 엔드포인트
| 메뉴 | 엔드포인트 |
|------|-----------|
| 업무일지 생성 | `POST /api/work/create` |
| 업무일지 조회 | `POST /api/work/read` |
| 업무일지 수정 | `POST /api/work/update` |
| 업무일지 삭제 | `POST /api/work/delete` |
| 카테고리 목록 | `GET /api/work/categories` |
| 급여명세서 목록 | `GET /api/salary/get_list` |
| 급여명세서 열기 | `POST /api/salary/open_file` |
| 급여명세서 업로드 | `POST /api/salary/upload_check` |

### works 테이블 스키마
```sql
CREATE TABLE IF NOT EXISTS works (
  id            SERIAL PRIMARY KEY,
  category      VARCHAR(50),
  title         VARCHAR(500) NOT NULL,
  content       TEXT,
  ex_start_date TIMESTAMP,
  ex_end_date   TIMESTAMP,
  start_date    TIMESTAMP,
  end_date      TIMESTAMP,
  progress      INT DEFAULT 0,
  create_date   TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  update_date   TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  status        INT DEFAULT 0
)
```

### 진행 상태 필터 로직 (Go)
- `status=0` : 활성(삭제 안됨)
- `status=1` : 삭제(소프트 삭제)
- 조회 필터 `status=2~5` : Go 메모리 내에서 `calcExpected()` 결과로 분류
  - `2` = 진행중 (expected 0~99, progress < 100)
  - `3` = 예정 (expected = -1, 시작 전)
  - `4` = 지연 (expected = 100, progress < 100)
  - `5` = 완료 (progress = 100)

---

## 빌드 검증
- `go build ./...` — 오류 없음
- `npx tsc --noEmit` — 타입 오류 없음
- `npm run build` — ✓ 88 모듈 변환, 번들 크기 ~239KB
